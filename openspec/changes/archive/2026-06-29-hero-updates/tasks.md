## 1. Global Styles Configuration

- [x] 1.1 Add the `.typewriter-frontend` CSS class and `@keyframes typing-frontend` to `src/index.css`.

## 2. Hero Component Integration

- [x] 2.1 Remove the neon pink glow (`drop-shadow-[0_0_12px_#f72585]`) from the title in `src/components/Hero.jsx`.
- [x] 2.2 Update the subtitle text to "Desarrollador Web Front-End" and apply the `typewriter-frontend` and `font-mono` classes in `src/components/Hero.jsx`.

## 3. Verification

- [x] 3.1 Verify that the neon glow is removed and the subtitle typing animation renders correctly.
