## 1. Global Styles Configuration

- [x] 1.1 Define the `.grid-bg` CSS class in `src/index.css` using overlapping linear gradients (cyan grid lines) and a radial gradient (vignette overlay).

## 2. Main Layout Integration

- [x] 2.1 Apply the `grid-bg` utility class to the main outer container in `src/App.jsx`.

## 3. Verification

- [x] 3.1 Verify that the grid background renders correctly in the browser and scrolls smoothly.
